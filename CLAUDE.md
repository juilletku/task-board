# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Git運用ルール

- コードを変更したら、そのたびにコミットを作成し、GitHubにプッシュすること。変更を未プッシュのまま放置しない。
- コミットメッセージは変更内容が分かるように簡潔に書くこと。
- プッシュ前に `git status` / `git diff` で変更内容を確認すること。
- force push や履歴を書き換える操作(`git reset --hard`、`git push --force` など)はユーザーの明示的な許可なく行わないこと。

## プロジェクトの状態

現時点でこのディレクトリにはファイルが存在せず、Gitリポジトリも未初期化です。プロジェクトのコードが追加され次第、このCLAUDE.mdにビルド・テスト・実行コマンドやアーキテクチャ概要を追記してください。
